{
  "env": {
    "name": "#{env_name}"
  },
  "api": {
    "baseUrl": "#{api_baseUrl}"
  },
  "uiBackendApi": {
    "baseUrl": "#{uiBackendApi_baseUrl}"
  },
  "auth": {
    "baseUrl": "#{auth_baseUrl}",
    "realm": "#{auth_realm}",
    "clientId": "#{auth_clientId}"
  },
  "legal": {
    "version": "#{legal_version}",
    "copyrightOwner": "#{legal_copyrightOwner}"
  },
  "features": {
    "v2": {
      "activated": "#{features_v2_activated}"
    }
  },
  "mock": {
    "terminology": false,
    "query": false,
    "result": false
  }
}
